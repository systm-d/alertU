alertu (0.3.0-1) unstable; urgency=medium

  * Ships a polkit rule. Without it the daemon's `loginctl lock-session` was
    refused with "Interactive authentication required", so the screen never
    locked on a stock install. polkit is now a hard dependency.
  * The daemon's account joins the `audio` group and a new `alsa_device`
    setting routes the alarm at the hardware, so the siren is audible from a
    system service. Playback failures are logged instead of discarded.
  * Enables the tray for every account with `systemctl --global enable`, and
    ships a pkexec helper behind the settings window's "Authorize this
    account" button, replacing the manual `usermod` step.
  * Guided pairing: press a button on the remote and AlertU records both the
    device and the key it sends.

 -- Kevin Delfour <kevin@delfour.co>  Sat, 25 Jul 2026 18:25:00 +0200

alertu (0.2.0-1) unstable; urgency=medium

  * No change to the Debian package itself. This release adds a Fedora RPM
    built from the same sources, so the staging script that rewrites unit
    paths and generates the sounds now lives at packaging/stage.sh and writes
    to target/pkg-staging rather than target/deb-staging.
  * The release workflow builds, installs and verifies both packages.

 -- Kevin Delfour <kevin@delfour.co>  Wed, 22 Jul 2026 19:40:00 +0200

alertu (0.1.0-1) unstable; urgency=medium

  * Initial Debian package.
  * Ships the daemon, the tray, the settings window and alertu-ctl, the
    system unit, the user unit for the tray, the generated sounds and the
    desktop entry.
  * No configuration file: the daemon writes /etc/alertu/config.toml itself
    on first use, so nothing here is a conffile.

 -- Kevin Delfour <kevin@delfour.co>  Tue, 21 Jul 2026 10:12:00 +0200
